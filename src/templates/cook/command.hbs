import { BaseCommand } from "@commands/BaseCommand";
import chalk from "chalk";
import ora from "ora";

/**
* {{properCase name}}Command implements a CLI command.
*
* This file was generated by the plop generator.
*
* Customize this command by editing the handleAction method.
*/
export class {{properCase name}}Command extends BaseCommand {
protected commandName = "{{camelCase name}}";
protected commandDescription = "Describe what the command does here";
protected commandArgument = "[args]";
protected argumentDescription = "Optional arguments for the command";

constructor() {
super();
this.configureCommand();
}

/**
* Executes the command's action.
*
* @param args - Optional arguments passed to the command.
*/
public async handleAction(...args: unknown[]): Promise<void> {
    const spinner = ora("Executing command...").start();
    try {
    // TODO: Implement your command logic here.
    // This is where you'll process the arguments and perform actions.
    spinner.succeed(chalk.green("Command executed successfully!"));
    } catch (error) {
    spinner.fail(chalk.red("Command execution failed."));
    console.error(error);
    }
    }
    }
